apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-api
  namespace: fcg-tech-fase-2
  labels:
    app: users-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: users-api
  template:
    metadata:
      labels:
        app: users-api
    spec:
      initContainers:
      - name: wait-postgres
        image: busybox:1.36
        command: ['sh', '-c', 'echo "Aguardando PostgreSQL..."; until nc -z postgres 5432; do echo "PostgreSQL indisponível - aguardando..."; sleep 3; done; echo "PostgreSQL está pronto!"']
      - name: wait-rabbitmq
        image: busybox:1.36
        command: ['sh', '-c', 'echo "Aguardando RabbitMQ..."; until nc -z rabbitmq 5672; do echo "RabbitMQ indisponível - aguardando..."; sleep 3; done; echo "RabbitMQ está pronto!"']
      containers:
      - name: users-api
        image: davidjmarinho/users-api:v1
        imagePullPolicy: Always
        ports:
        - containerPort: 80
          name: http
        env:
        # Override ConnectionStrings.DefaultConnection from appsettings.json
        - name: ConnectionStrings__DefaultConnection
          value: "Host=postgres;Port=5432;Database=fcg_users_db;Username=fcg;Password=fcgpw"
        # Override RabbitMq config from appsettings.json
        - name: RabbitMq__Host
          value: rabbitmq
        # Application Configuration
        - name: ASPNETCORE_ENVIRONMENT
          value: Production
        - name: ASPNETCORE_URLS
          value: http://+:80
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: users-api
  namespace: fcg-tech-fase-2
spec:
  type: ClusterIP
  selector:
    app: users-api
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
